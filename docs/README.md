---
tags: [index, handbook]
type: index
status: published
updated: 2026-09-25
---

# AI Agent 手册 · AI Agent Handbook

![AI Agent 学习手册 · 从原理到生产](.gitbook/assets/banner-home.svg)

**从原理到生产**：LLM 与注意力 · Agent 循环 · 工具协议 · 记忆与 RAG · 规划 · 多智能体 · 评估与安全 · 工程化 · AI 基础设施 · 具身智能

**From theory to production**: LLMs & attention · agent loops · tool protocols · memory & RAG · planning · multi-agent · evaluation & safety · engineering · AI infrastructure · embodied AI · **2026 frontier**

> **中文**：一本持续更新的开源 AI Agent 手册与资源库：196 页 / 19 章，含 2026-09 前沿（GPT-6 Astra、Agents API、Claude Fable 5.1、System One 决策模型）与 6 个可离线真实运行的动手实验。每一页都要求「把话说实」——原理给出公式与推导、结论给出可点开的出处、流程给成分步演示与可点标签（全书正文不贴可执行代码，接口只给真实字段契约）。
>
> **English**: A continuously updated, open-source AI Agent handbook and resource library — 196 pages across 19 chapters, including a 2026 frontier chapter (GPT-6 Astra, Agents API, Claude Fable 5.1, **System One decision models**), 108 of them carrying rendered formulas, plus 6 offline-runnable hands-on labs and an index of 323 open-source projects. Every page has to show its work: principles come with derivations, claims link to primary sources, and processes are stepped out as interactive walkthroughs rather than pasted code.

## 快速开始 · Quick start

| 你是 · You are | 路线 · Route | 通关标准 · Done when |
|---|---|---|
| 只会用 ChatGPT · New to LLMs | 01 → 02 → 03 → 04 | 能讲清「Agent 与 Chatbot 的区别」· explain agent vs chatbot |
| 有开发经验 · Engineer | 02 → 05 → 06 → 09 → 11 | 能写出会调工具、能恢复错误的 Agent · ship an agent with tools & recovery |
| 关注前沿与论文 · Researcher | 04 → 08 → 10 | 能读懂 ReAct 并复现核心循环 · reproduce the ReAct loop |
| 要搭团队底座 · Platform / Infra | 03 → 11 → 16 | 能给出 TTFT/TPOT/缓存命中率/成本基线 · set cost & latency baselines |
| 做机器人或想转具身 · Robotics | 02 → 04 → 17 → 16 | 能跑通「感知→技能→执行」闭环 · close a perceive→skill→act loop |
| 追前沿与选型 · Frontier / Buyer | 02 → 18 → 10 → 12 | 能读懂 Astra / Agents API / Fable 5.1 对比并做 harness 选型 · pick a 2026 stack |
| 想动手写代码 · Hands-on | 02 → 04 → 06 → 19 | 能离线跑通 6 个实验、把 MockLLM 换成真 API · run all 6 labs offline, then swap in a real model |

完整路线、前置知识与自检标准 · Full paths, prerequisites & self-checks：**[学习路线 Learning path](00-index/learning-path.md)**

![学习路线：四个阶段 · Four-stage learning path](.gitbook/assets/00-learning-path.svg)

*《图：四阶段路线是按「能否自检」切分的——每块底部写着过关标准，达不到就别进下一阶段》*
## 边学边做 · Hands-on labs

第 19 章是 6 个**纯标准库、零 API Key、离线可跑**的动手实验：手写最小 ReAct 闭环 → 迷你 BM25 RAG → 迷你 MCP 服务 → 三角色协作 → 评测与 trace → 护栏与接真模型。页面里不放代码，改成一步步的**分步演示**与**可点标签**（对照组、失败分支都能点开看），配的每一份输出都是本机真跑出来的原文——第 54 轮把这句话逐条复跑核对过：六页共 95 个引用数字，60 个直接对上默认运行，剩下 35 个逐条追查后 33 个用「把改动重新做一遍」复现（lab6 的 3/8=38% 误伤与 2/4 漏防、lab4 的消息数 7→9 与总线 298→303 字、lab3 的 -32601 报文与三工具发现行、lab2 三组标签与两个 b 值的完整排序），2 个是判据假阳性（`"temperature": 0.2` 那类写在 JSON 契约里的字段本就不是运行输出）；核对过程中修掉了 lab3 服务端一处真 bug。脚本自己在仓库 `labs/` 下，六个文件各自自包含，看懂后把 MockLLM 换成真 API 主循环一行不改。

👉 **[进入动手实验 · Start the labs](19-labs/README.md)**

## 必读 12 个 · Top 12 must-reads

| 资源 · Resource | 一句话 · In one line |
|---|---|
| [2026 前沿模型地图](18-frontier-2026/frontier-models-2026.md) | GPT-6 Astra 与 Claude Fable 5.1 怎么比、怎么买 |
| [OpenAI Agents API](18-frontier-2026/openai-agents-api.md) | 托管 Codex harness：压缩 / tool search / 子 Agent |
| [ReAct 论文](13-resources/papers/react.md) | 现代 Agent 循环的思想源头 · the origin of the think-act-observe loop |
| [Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents) | 什么时候用工作流、什么时候才该上 Agent · workflow vs agent, and when |
| [Attention Is All You Need](https://arxiv.org/abs/1706.03762) | Transformer 原始论文，一切 LLM 的底座 · the paper every LLM builds on |
| [DeepSeek-R1](https://arxiv.org/abs/2501.12948) | 用可验证奖励做 RL，训出推理能力 · RL with verifiable rewards |
| [Pi Agent](https://github.com/earendil-works/pi) | 极简 harness 的活教材 · a minimal harness you can read end to end |
| [Claude Agent SDK](18-frontier-2026/claude-agent-sdk.md) | 把 Claude Code 内核嵌进自己进程 |
| [MCP 官方文档](https://modelcontextprotocol.io/docs/learn/architecture) | 工具接入的事实标准 · the de-facto tool protocol |
| [AI Agents for Beginners](13-resources/courses/ai-agents-for-beginners.md) | 微软官方免费入门课 · Microsoft's free beginner course |
| [SWE-bench](13-resources/benchmarks/swe-bench.md) | 编程 Agent 的标准考场 · the standard arena for coding agents |
| [评估 2026](18-frontier-2026/eval-2026.md) | Terminal-Bench 4.0 / OSWorld 2.0 读榜纪律 |

## 按主题挑资源 · Browse by topic

**2026 前沿 · Frontier** · [本章导读](18-frontier-2026/README.md) · [模型地图](18-frontier-2026/frontier-models-2026.md) · [Agents API](18-frontier-2026/openai-agents-api.md) · [Agent SDK](18-frontier-2026/claude-agent-sdk.md) · [Harness 分类学](18-frontier-2026/model-native-vs-harness.md) · [评估 2026](18-frontier-2026/eval-2026.md) · [安全 2026](10-evaluation-safety/safety-incidents-2026.md)

**框架与编排 · Frameworks** · [LangChain](09-frameworks/langchain.md) · [LangGraph](09-frameworks/langgraph.md) · [AutoGen](09-frameworks/autogen.md) · [CrewAI](09-frameworks/crewai.md) · [DSPy](09-frameworks/dspy.md) · [LlamaIndex](09-frameworks/llamaindex.md) · [Semantic Kernel](09-frameworks/semantic-kernel.md) · [OpenAI Agents SDK](09-frameworks/openai-agents-sdk.md) · [Claude Agent SDK](18-frontier-2026/claude-agent-sdk.md) · [完整项目索引 Full index](13-resources/projects/README.md)

**编程 Agent 与 Harness · Coding agents** · [编程 Agent 案例](12-applications/coding-agent.md) · [deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) · [Pi](https://github.com/earendil-works/pi) · [Codex](https://github.com/openai/codex) · [Gemini CLI](https://github.com/google-gemini/gemini-cli) · [SWE-agent](https://github.com/SWE-agent/SWE-agent) · [Aider](https://github.com/Aider-AI/aider) · [Claude Code 提示词全集](https://github.com/Piebald-AI/claude-code-system-prompts)

**记忆与 RAG · Memory & RAG** · [RAG 基础](06-memory-rag/rag-basics.md) · [Embedding 与相似度](06-memory-rag/embedding-similarity.md) · [向量数据库](06-memory-rag/vector-database.md) · [GraphRAG](06-memory-rag/graphrag.md) · [知识图谱](06-memory-rag/knowledge-graph.md) · [LlamaIndex](https://github.com/run-llama/llama_index) · [RAGFlow](https://github.com/infiniflow/ragflow) · [Mem0](https://github.com/mem0ai/mem0) · [Milvus](https://github.com/milvus-io/milvus) · [Qdrant](https://github.com/qdrant/qdrant) · [pgvector](https://github.com/pgvector/pgvector) · [FAISS](https://github.com/facebookresearch/faiss)

**工具与协议 · Tools & protocols** · [Function Calling](05-tool-protocol/function-calling.md) · [MCP](05-tool-protocol/mcp.md) · [A2A](05-tool-protocol/a2a.md) · [工具权限与沙箱](05-tool-protocol/tool-permission-sandbox.md) · [MCP Servers](https://github.com/modelcontextprotocol/servers) · [MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk) · [A2A 仓库](https://github.com/a2aproject/A2A)

**浏览器与 Computer Use · Browser & computer use** · [Computer Use / Browser Use](05-tool-protocol/computer-use-browser-use.md) · [Computer Use 2026](18-frontier-2026/computer-use-2026.md) · [通用 Agent 产品](12-applications/general-agent-products.md) · [browser-use](https://github.com/browser-use/browser-use) · [Playwright MCP](https://github.com/microsoft/playwright-mcp) · [Stagehand](https://github.com/browserbase/stagehand) · [cua](https://github.com/trycua/cua)

**评估、安全与对齐 · Evaluation & safety** · [Agent 评估指标](10-evaluation-safety/evaluation-metrics.md) · [基准测试总览](10-evaluation-safety/benchmarks.md) · [评估 2026](18-frontier-2026/eval-2026.md) · [2026 安全现实](10-evaluation-safety/safety-incidents-2026.md) · [幻觉问题](10-evaluation-safety/hallucination.md) · [提示注入](10-evaluation-safety/prompt-injection.md) · [越狱攻击](10-evaluation-safety/jailbreak.md) · [权限与沙箱](10-evaluation-safety/permission-sandbox.md)

**推理与部署 · Inference & deployment** · [推理、量化与部署](03-llm/inference-quantization-deployment.md) · [vLLM](https://github.com/vllm-project/vllm) · [llama.cpp](https://github.com/ggml-org/llama.cpp) · [Ollama](https://github.com/ollama/ollama) · [LiteLLM](https://github.com/BerriAI/litellm) · [unsloth](https://github.com/unslothai/unsloth) · [LlamaFactory](https://github.com/hiyouga/LlamaFactory) · [TRL](https://github.com/huggingface/trl)

**沙箱与运行时 · Sandboxes & runtimes** · [权限控制与沙箱隔离](10-evaluation-safety/permission-sandbox.md) · [E2B](https://github.com/e2b-dev/E2B) · [Firecracker](https://github.com/firecracker-microvm/firecracker) · [agent-infra/sandbox](https://github.com/agent-infra/sandbox)

**基准与数据集 · Benchmarks & datasets** · [SWE-bench](13-resources/benchmarks/swe-bench.md) · [GAIA](13-resources/benchmarks/gaia.md) · [WebArena](13-resources/benchmarks/webarena.md) · [AgentBench](https://github.com/THUDM/AgentBench) · [ToolBench / BFCL](https://gorilla.cs.berkeley.edu/leaderboard) · [τ-bench](https://github.com/sierra-research/tau-bench) · [OSWorld](https://osworld-v1.xlang.ai/) · [数据集索引](13-resources/datasets/README.md)

**论文 · Papers** · [ReAct](https://arxiv.org/abs/2210.03629) · [CoT](https://arxiv.org/abs/2201.11903) · [ToT](https://arxiv.org/abs/2305.10601) · [Reflexion](https://arxiv.org/abs/2303.11366) · [Toolformer](https://arxiv.org/abs/2302.04761) · [InstructGPT / RLHF](https://arxiv.org/abs/2203.02155) · [DPO](https://arxiv.org/abs/2305.18290) · [LoRA](https://arxiv.org/abs/2106.09685) · [RAG](https://arxiv.org/abs/2005.11401) · [GraphRAG](https://arxiv.org/abs/2404.16130) · [MemGPT](https://arxiv.org/abs/2310.08560) · [SWE-bench](https://arxiv.org/abs/2310.06770) · [论文索引](13-resources/papers/README.md)

**课程、博客与社区 · Courses, blogs & communities** · [AI Agents for Beginners](13-resources/courses/README.md) · [LangChain Academy](https://academy.langchain.com) · [HF Agents Course](https://huggingface.co/learn/agents-course) · [Anthropic 工程博客](https://www.anthropic.com/engineering) · [Lilian Weng](https://lilianweng.github.io/) · [Simon Willison](https://simonwillison.net/) · [社区清单](13-resources/communities/README.md) · [Awesome 列表](13-resources/awesome-lists/README.md)

## 关于这本手册 · About this handbook

- **196 页 / 19 章**（含 2026 前沿章 + 6 个离线动手实验）· 196 pages across 19 chapters。口径说清：这 196 是**已发布页面数**（本地 `docs/` 下 `.md` 减 `SUMMARY.md` 与截图清单），含首页与 3 个模板/指南页；纯正文是 193 篇。与线上 `llms.txt` 的页面数一一对过，不是估的
- **108 篇页面含 KaTeX 公式，共 927 条**（判据**已落库可复跑**：`tools/checks/check_katex_formulas.py` + `katex_render.mjs`，与线上结构对平轴**共用同一个分词器**——先剥围栏与行内代码，独占一行的 `$$` 才算展示块，其余 `$$` 在**同一段落内**配对，然后逐条过真 `katex@0.18.7` `renderToString(throwOnError)`（版本钉在 `tools/checks/data/katex/package.json`，第 59 轮起：解析到别的构建就 `exit 2` 不出数——线上不吐 KaTeX 版本号，实测整页 HTML 里 `katex` 只在类名里，所以这条轴钉本地而不是对齐线上），**0 解析失败**；范围是 193 篇正文，含本页与更新日志，模板页代码块里的示例写法不算。把更新日志页排除则是 107 篇 / 917 条（第 60 轮：5 处跨页复述的公式改成指向后 928→927，被指向的权威页一条没动）。公式数**只认这一个判据脚本**的读数——第 56 轮及以前它只存在于临时目录，同一棵树因此同时有过 930 / 931 / 943 三个数，本轮起它进了仓库，那三个读数全部作废）· 108 pages carry rendered formulas
- **数字与结论都能点回一手来源**，文末「参考资料」只放外部来源 · every claim links back to a primary source
- **每章三件套：18 章「读完能做到」共 89 条 + 17 章章末自测共 51 题（题题给出处）+ 17 章术语速查共 198 行**：一句话的主张是「全量覆盖，题目可答、答案有出处」，第 78 轮起这句话有判据可复跑：`tools/checks/check_chapter_extras.py`，读数 `chapters=18 goals=18 quizzes=17 quiz_exempt=1 questions=51 with_source=51 glossaries=18 terms=198 findings=0`。**「题目可答」被拆成两件可判的事**：每题必须点一个出处（`见 xxx.md` 或一条 Markdown 链接），而那个文件必须真在仓库里——51/51 通过、死链 0；光有问号没有出处的问题买不到覆盖。**唯一没带自测的那一章是术语表**，而这不是本轮抓到的缺陷：它页面里本来就写着「本章是纯查表页，不出自测题——取而代之，记住用它的三条路」，所以判据把豁免**钉在那句话上**而不是钉在一份名单上——谁把那行删掉，这一章立刻改判 `EXEMPT-UNSUPPORTED`，想悄悄多豁免一章不行。同一趟还抓到两件事：① **判据首跑把两行合法的术语表行判成坏行**，因为白话列里写了带反斜杠转义的竖线，按竖线硬切就凭空多出两列——改成只切未转义的分隔符，并常驻一条「带转义竖线的行必须读干净」的反例（第 57 轮「标签属性里的裸 `%`」那类尺子错的又一次现身）；② 一行术语的中文列只写了光杆的拉丁缩写，全书 198 行里只有它这一行中文列没有中文，按本书「没有通用译名就给一句括注」的既有写法补全。**没有为凑覆盖率给术语章塞一道自测题**
- **257 个 GitBook 原生提示卡 + 54 组分步演示（279 步）+ 52 组可点标签（195 个页签）**：一句话结论 / 易踩的坑 / 风险警示分色呈现，流程与分支结局用交互组件逐步走完（逐文件点数，代码块与行内代码里的写法示例不计入——被这条口径排除掉的写法示例由判据自己逐名打印，本页故意不抄这些数——第 77 轮写这条统计时它们在同一次运行里动了三次（提示卡 23→24、标签组 11→12、页签 7→8），因为解释这套语法的句子必须把语法本身写进反引号；一个连本轮自己都能推动的数印在首页上，只会变成下一轮的假数字）。这五个数从第 77 轮起有判据可复跑：`tools/checks/check_widget_pairing.py` 的 inventory 腿，它把**首页这句话里的数字解析出来**再与树对平，并复用配平腿那同一个分词器与同一套围栏/行内代码剔除——所以「配对 0 问题」与「数出另一个数」不可能同时发生，两条判决共用一把尺。**这里钉等号而不是地板**（第 76 轮刚为反例定下「措辞能推动的数不配当等号」）：首页有义务跟着树走，读者抄不走它的数，因为数就印在句子里。**为什么要新落一条判据**：句中那个 251 是第 54 轮一次手抄，此后 20 多轮里树长到 257 而这句话一个字没动，直到本轮才被发现——这是「README 印着一个没有判据的数」第三次现身（第 57 轮：判据脚本压根不在仓库里；第 76 轮：图注那三个数从没被任何脚本产出过）。同一趟还把这句话没提到的家族一起报出（可展开块那 1 处写法示例，以及六个标签名各自的逐名点数），新家族出现时不会静默漏计；地板另有两条（提示卡 ≥250、分步演示组 ≥50），绿色由 8 个种植反例撑着：真实的一处提示卡必须被点到（正控制）、围栏里的一处与行内代码里的一处必须**不**计入、五个结束标签合起来不构成任何一次开启、组与页签的归属必须读成「1 组 2 页签」、带 `-` 空位控制的写法必须照点、属性里含裸 `%` 的标题必须照点、一个分步演示套两步必须读成「1 组 2 步」
- **正文零可执行代码，而且每份数据契约都真的过了解析**：需要动手的地方给的是**真实字段契约**（`json`/`yaml`）与分步演示；六个实验的脚本仍在仓库 `labs/` 下可离线真跑，页面按路径指过去——**这句从第 80 轮起是真的被跑出来的**：`tools/checks/check_lab_runnability.py` 把六个脚本各**连跑两遍**（在临时副本里，免得它们把 `traces.jsonl` 写进仓库），要求退出码 0、stdout 非空、两遍**逐字节相同**，并且 `labs/out/labN.out` 那六份**入库记录必须等于今天重跑的输出**。读数 `labs=6 exit0=6 deterministic=6 stdout_lines=141 worst=0.63s pages=6 pages_naming_script=6 records_current=6/6 findings=0`；控制 6 条：一份干净样本必须不报，退出码 3、只退出不出声、未播种的随机、以及**一份过期的入库记录**必须各自被抓，而一份与记录相符的必须不报。这一趟顺手抓到一件旧账：`lab5` 的瀑布图把 `start` 读自真实时钟，`sleep` 回来的那点余量每次不同，**有一行的前导空格会漂**——第 71 轮那批日志当时就记了「非种子能定」，本轮改成走**模拟时钟**（上一条的 `start + dur`），六份入库记录逐一与今天重跑的输出对过：只有 lab5 的两行随之一变（`c1 answer` 的条长按模拟时钟重算），其余五份一字不差。第 54 轮据此清零了 55 页 / 60 块 / 1502 行代码，同时正文净增 12.1 万字符（新增 17.7 万、删除 5.6 万，剥掉 Markdown 符号后按字符计，58 个被改文件）。**这句话从第 79 轮起有判据**：`tools/checks/check_structure.py` 新增两条判决——`CODEFENCE`（围栏的语言标签不在数据白名单里就判红）与 `JSONCONTRACT`（每个 json 块必须真过 `json.loads`，此前「每份都过解析」也只是句好话）。全站读数 `mermaid=217 json=79 text=60 markdown=11 yaml=10 无标签=9 | executable-tagged=0 json contracts parsed=79 unclosed=0`（**钉的等号是 `executable-tagged=0` 与 `unclosed=0` 这两条不变量，不是前面五个数**——那五个是「写一段解释围栏语法的文字」就能推动的计数，第 83 次日志加了一个无标签示例它们就 8→9 动，第 84 次日志贴自己的量测读数时加了三个 `text` 围栏所以 52→55，同轮第一次收尾贴判决加一次 55→56、第二次收尾（线上复验那一节）再加一次 56→57。上面那串读数取的是**第二次收尾之后**的那一趟，而它下面写的每一句都可能是下一次推动它的东西）。两侧控制常驻：`python`/`bash`/`js`/`sql`/`console` 五种必须被抓，六种数据标签必须不抓；一份带尾逗号的 json 必须被抓，一份合法的必须不抓。**同一趟修掉的是这两条判决脚下的尺子**：仓库里两个共用的围栏分词器把「任意一行同字符的围栏」当成闭合，而 CommonMark 要求闭合行**不带语言标签、且不短于开场**——于是模板页那种「用四个反引号包住含三个反引号示例」的写法被从中间截断：示例内容被读成正文（可能凭空生出判决），真正的正文被读成示例（可能吞掉判决）。改之前先问读者那一侧：**线上页面把那些行包在 `highlight-line` 里**，也就是平台按 CommonMark 渲染，不是按旧尺子。改完之后新旧两把尺子在这张真实页面上确实读数不同（13 行），这条差异钉成常驻反例——将来谁把规则「简化」回去，判据立刻红而不是静默。附带一条只有新尺子判对的：**模板页里那个示例 json 不该被当成真契约**（旧尺子以为块在示例中间就闭合了，会把它当契约去解析）
- **读者能点的每一处链接都被机器查过**：第 1 / 13 / 17 / 21 / 22 / 25 轮各审过这条轴的一段，用的都是当场写、跑完就扔的脚本——所以「断链与孤儿页」在 80 轮里其实是计划书上的一行，而不是判决。第 81 轮起它常驻 `tools/checks/check_link_graph.py`（站内跳转图 + 外部来源缓存判决 + 平台侧锚点腿）。三条腿按「这条证据是关于书的、还是关于探测器的」分工：`DEAD` / `SOFT-404` / `MOVED` 是书的病，`BLOCKED` / `NET` / `UNREADABLE` 只许报「判不了」，被引用而缓存里没判决的 URL 直接算 `UNCHECKED` 覆盖失败而不是通过。全站读数 `link-graph: pages=196 relative=1582 external=1526 distinct=717 ok=702 blocked=14 net=1 unchecked=0 fragments=0`（第 86 轮重新锚定：上一版写 1578，而 HEAD 那一棵树实测已经是 1581——**这条徽章红过三轮**，因为第 85 轮自己列的尾腿清单里恰好不含 `link-graph`，也就是不含「会随文档改动自己挪数」的那类判据）——**这句里的数字由判据自己从首页解析出来与树对平**（写错一个数即 `HOMEPAGE-BADGE`，把这句删掉即 `HOMEPAGE-BLIND`，与第 77 轮那三个数共用同一条思路）。首趟量出 22 个一手来源地址已搬家（51 处引用 / 34 页，含 1 条真 404），**每条替换都过采纳闸门**：新地址必须自己探到 200 且**落点等于自身**——照抄浏览器跳转链的第一跳会把读者送到一个不是那篇文档的页面。**14 条 `BLOCKED` + 1 条 `NET` 是欠账不是通过**：反爬与需登录的主机（`openai.com/news/`、`www.reddit.com/r/LocalLLaMA/`、`www.iso.org/`、`academic.oup.com`、`dl.acm.org`…）每轮都会被 `--refresh` 重试，缓存里只冻结确定性判决。锚点腿读 `fragments=0` 是**量出来的空集**（全书引用的是文件，不是文件里的某一节），这条腿自带 4 条 `BLIND` 判决——目标页须在 `llms.txt` 里有唯一条目、服务端须真发 heading id、正控制须命中、一句幽灵 fragment 须不命中，平台哪天不发 id 它会报瞎而不是报绿；**离线不猜 fragment 是标定过的**：更新日志页 252 个标题、服务端发布 243 个 id，而按 CommonMark 直觉造的 slug 命中 0/252（GitBook 把中文标题转拼音、给数字开头的标题加 `id-` 前缀、并且不给 H1 发 id）。绿色由 `--selftest` 的种植控制撑着，其中三桶是本轮踩坑之后加的：**`404 Page Not Found` 必须被抓，而 arXiv 编号 `[2404.06654] RULER: …` 必须不抓**（第一趟就是被这个假阳性骗出 3 条死链的）；只去掉 `www` 不算搬家，但换掉文档主机必须算（`docs.smith.langchain.com → docs.langchain.com` 是本轮真搬家的一族）；四条覆盖地板必须各自能出声——**一个扫不到链接的判据最擅长的就是报绿**：只认 `[标签](地址)` 的旧尺子把整张资源索引表读成散文，98 处可点跳转 / 54 个去重地址在它眼里根本不存在
- **每一句正文都真的到了读者眼前**：`tools/checks/check_prose_survival.py` 把 196 篇正文的句子单元（`##` 及更深层的标题、`>` 引用块里的句子自第 88 轮起也是单元）与**线上页面**的可见正文逐条比对。已有轴里没有一条比这个：SSR 对平轴比的是组件数量、图片轴比的是图、链接轴比的是地址——**一句话少半截，它们全部照绿**（第 76 轮那次「转义反引号把整句渲染成空公式」就是人眼发现的）。全站读数 `pages=196 units=13923 plain=11691 heading=640 quote=57 widget=1535 | MISS=3 REVISION-BEHIND=17 STALE-COPY=60`（**第 88 轮推送前实测**；随后写进日志与首页的字还会推动 `units`、`plain`、`heading`，所以这几条只到那次复跑为止，措辞推不动的等号只有「同步完成后 `MISS=0` 且 `REVISION-BEHIND=0`」）。这一趟的三个非零数**全是本轮自己还没推上去的字**：3 条 `MISS` 与 17 条 `REVISION-BEHIND` 都在首页这一句所在的 `docs/README.md:95`（首页没有轮次号历史，判据给不出切点、只能整页判，于是未同步的本地编辑读成丢失——这不是本轮的缺陷，是「同步前」的形状），60 条 `STALE-COPY` 是更新日志新增那一节坐在发布稿尚未到达的位置上。**线上追上之后这三个数必须回到 0/0/60→个位**，那一趟才是本轮的收尾断言。那 17 条 `REVISION-BEHIND` 不是吞句——线上那份页面仍印着本轮改**之前**的句子，判据拿发布稿自己印出的旧字为每一处作证，这个桶第 88 轮才有）：**同步前那一趟它自己报过一条 MISS**，红在本轮改过的 `agent-failure-playbook.md:56`——它把加粗标记从「完成」前面挪到「可验证定义」前面，而 `norm` 把星号归一成空格，于是这条轴**看得见标记挪到了哪、看不见标记有没有生效**（这条分辨率边界正是第 84 轮量出来的，也是那一轮新轴的来由）；同步后它一个字都不报。收尾那趟还把 311 条 STALE-COPY 一起归零（线上那份更新日志已发到本轮，`check_live_sync` 两腿各读 `newest round=84`）。**首趟 1923 条红里只有 1 条是书的病**：四次归因抓到的都是尺子（GitBook 会在两个表格单元格之间印自己的 `gitbook assistant`、`&quot;` 实体的解码顺序、CSS 生成的有序列表序号、裸 `<https://…>` 自动链接把 URL 本身印进正文），第五次是 `<[^>]+>` 把更新日志自己那句 `（W<420 且 H>380）` 的尖括号当成标签吃掉。**唯一那条真缺陷**：平台在一句话里把 `**` 配成「第一个配最后一个」，于是 20 个从没打算加粗的字符连同链接标签一起被粗化。它被判成书的病而不是尺子的错，靠的是「配对正确时 `</strong>` 的边界恰好落在 `**` 归一化成的那个空格上」——全书 865 行「≥2 个加粗对」（其中 121 行同时带 Markdown 链接）在这条边界判定下**全部判绿**，只有那一行对不上。修法是把那句拆成两句、链接挪到句末独立指路，**没有为它放宽 `*` 的处理**（那会判坏 865 行的常见情况，顺带把这条真缺陷藏起来）。绿色由 21 条种植控制（A–U：吞句是唯一 MISS、围栏体永不作散文、组件体打标、frontmatter 不判、真单元格在注入按钮文本后仍存活、`&quot;` 句不算吞、**不带分号的 `&quot` 必须原样留在单元里**——第 84 轮收尾时作者侧解码器解码了平台没解码的遗留实体写法，把读者明明看得见的 `quot` 从单元里删掉，两侧对同一串字符用了两套规则，现在作者侧只解码**以分号结尾**的引用、线上侧维持原样、有序标记不入单元、按节切点四态、自动链接终结单元、散文尖括号在原生与转义两侧都存活、第 87 轮撤掉的那条目录排除**必须撤着**（`walk()` 里 `14-templates/` 三页在场，且必须含 `style-guide.md` 本身）、旧措辞只落在**这一段**缺口里才免判这一处、缺口空着必须是 `MISS`、别段的旧字不许替这段吞句作保、锚点在页上不唯一就不给免、被平台按钮文本粘起来的表格单元格不是「新措辞」、树里已有的两段粘成一句也不算新话（要改判必须新增树里没有的词）、`##` 标题是一个独立单位而正文 `#` 永远不是、`>` 引用块里的句子是一个独立单位而围栏里演示的假引用与图片 `alt` 永远不是）+ 一条 `--mutate` 变异控制撑着：**在已下载的线上 HTML 里藏掉一句真散文的 8 个字符，判据必须点名它是 `MISS`、不许把它说成「这一版还没发布」**——这是那五处放宽唯一的辩护（尺子松了之后仍抓得住真丢的字）。读数 `controls: OK, every bucket able to fire`，而 `--selftest` 从第 88 轮起还跑一条**对平闹钟**：这一句里写的「多少篇正文」「多少条种植控制（哪几个字母）」必须由判据自己数出来再比对，谁悄悄缩了视野或加了一条控制却忘了这句，它就是红的。**留下的账四条**：① 切点之下的散文晚一轮才判（切点从页面自己印出的轮次序列推导，历史不是「最新在前」就不给切点、全量判）；第 88 轮起「同轮改动」不再只靠切点兜——`REVISION-BEHIND` 桶拿发布稿自己印出的旧字为那一处作证，本地就能区分「平台发的是另一版」与「渲染器吞了字」；② 曾经「没有 `<strong>` 覆盖范围的常驻轴」——「加粗范围错、句子仍然连续」的写法这条轴看不见，这次抓到是因为误配对顺带把句读打断了；**这条账第 84 轮销了**，接住它的是下面那条新轴；③ 更新日志页线上落后（第 88 轮实测 `MISS=0 REVISION-BEHIND=3`，三条是同一个 region：线上仍印着第 87 轮那段话改前的句子，`git` 里那句只存在于 `HEAD~1`）——**所以这条轴的收尾条件从第 88 轮起是 `MISS=0` 且 `REVISION-BEHIND=0`**，落后不再是「NOTE 一声就算过」；拆不拆这一页是**产品级取舍**，不归判据决定，本轮页面一个字没动；④ 分辨率地板 14 个归一字符：短于此的标题、引用与句子不判（两个字能撞上正文里任何地方，判了只会教人把尺子调瞎）。第 88 轮首跑按这条地板量过：围栏外 H2+ 标题行 2617 条，661 条过地板，584 行至少产出一个单元，77 条是纯代码/纯链接的短标题——它们到读者眼前的字仍由别的轴管，这条轴不判
- **加粗标记不会变成读者眼前的裸星号**：`tools/checks/check_emphasis_flanking.py`（第 84 轮）把 CommonMark 的 flanking 表落成两条腿——作者腿离线预测每一页会外翻几个 `**`，`--live` 腿要求**线上页面字面出现的 `**` 逐页等于预测数**（平台才是最终裁判，预测只是让下一轮能离线复现）。落库前先按第 82 轮的规矩把尺子锚在第三方实现上（`markdown-it-py 2.2.0`，CommonMark 的另一个独立构建）：修复前对 HEAD 的更新日志读 `predictor=8 reference=8`，修复后 196 个作者页逐页 `predictor=0 reference=0`。**首趟量出 9 页 24 个读者看得见的标记**，全部只挪标记位置修掉（8 个读者页 + 更新日志 4 处，**内容一个字没删**），复跑 `pages=0 leaked_strong_markers=0`，同步完成后 `--live` 腿逐页读 `pages=196 fetch-failures=0 prediction-mismatch=0 served_markers=0`——作者侧预测与读者眼前字面 `**` 的**逐页等式**成立（第 84 轮 §六 承诺的那两条收尾断言，量于线上而非本地）。这条轴的动机不是轴量出来的，是**第 83 轮那次真实渲染目检看见的**：同一页散文轴当时读 `MISS=0`——字确实到了读者眼前，而标记有没有生效本来不在它的对象里。绿色由 12 条种植控制撑着（`* 一条列表项` 这类列表标记必须不报，代码跨度、链接标签与公式乘号里的星号必须不报，真外翻必须报）；落库过程中这把尺子自己错过三次，三次都改在尺子上而不是内容上（打印每页只放 3 条于是 8 条里 5 条从没印出来过、退出码挂在永不到读者眼前的落单星号上、代码跨度替换成空格造出 277 个假阳性）。**留下的账**：`_` 与 `__`、三个以上星号不在对象里；更新日志那一处「组件写法嵌进加粗范围」是平台与 CommonMark 的分歧（n=1），没做成规则，由等式腿兜住下一例；`.md` 端点会把平台判定为字面的标记写成转义形式，那是一枚还没用起来的第三方信号
- **叫读者去看出处的句子，必须给可点的链接**：`tools/checks/check_source_pointers.py` 只认一种句子形状——同一个短句里既有「去看」的动词（`详见`/`参见`/`参考`/`见`，且不能是 `常见`、`看见` 的尾巴）又点了一份外部文件（官方、发布页、条款、论文、公告、SDK…），而这一行没有任何 Markdown 链接。全站读数 `pages=191 pointer_lines=3 findings=0`；同一判据对修复前的 HEAD 读 **5**、修复后读 **0**，真实页变异控制 `0 → 1 → 0`，锚点腿的正控制是**修前那段措辞渲染出 0 个 `<a href>`**（否则「6 个链接都在」与「一个都没渲染」同数）。这条轴是**收窄**出来的：先按「带数字却没链接」扫过一遍，读数 **247 处 / 71 页**，逐条看过才发现它把本书自己的工程经验值（`输入 token 常是输出的 10–50 倍`，根本无从引用）和别人的数字混为一谈——照它改只会逼作者在每句经验值后面挂不相干链接，**判据自己就成了灌水源**。判据内部还常驻 12 条 look-alike 反例（`错误原文回填给模型`、`（可含论文、官方文档、源码）`、`只参考有权限的文档`、`## 常见基准`、「（见本页末尾）」这类同页导航、项目卡片里的「详见仓库」——卡片首行本来就链着那个仓库）
- **全书 217 张 Mermaid 图经真解析器逐块校验**，0 渲染风险；每张图的自然宽度都用**与线上同版本**（mermaid 11.14.0）的引擎实测过，最宽 1116px。**「最宽 1116px，全部落在正文列宽（1120px）内，线上不会被缩放」这句从第 21 轮站到第 61 轮，是假的**：线上读者默认的正文列实测 **768px**（`layout-wide` 才给 1152，而它默认不开），Mermaid 出厂 `useMaxWidth: true`——SVG 带 `width="100%"` + `style="max-width: 自然宽"`，超宽的图连标签一起**缩小**塞进列里。按真实列宽重跑全站：102 张超宽、101 张被缩小、**0 张能横向滚动**，24 张标签实绘 <12px、最小 **11.0px**（正文 16px）。判据**第 58 轮起已落库可复跑**：`tools/checks/check_mermaid_geometry.py` 用无头 Edge 把 217 块**逐块真渲染**并读回 viewBox，引擎版本从线上页面 markup 里读出来与本地断言相等（不等就直接退出，不出数），每轮再埋两张必须被捉住的雷（一张 2420px 的超宽链、一张残缺语法）。判据自己也在第 62 轮被抓到过一次坏的：fixture 里 `--column` 形参没生效、永远按 1120 排版，于是报出「0 张缩放、217 张横向滚动」两条假话——现在同一趟会把 SVG 自己的 `width`/`style` 属性记回来，缩放还是滚动一次跑就能重验；列宽本身由 `tools/checks/check_live_column.py` 在线上量（500px 与 1100px 两个控制盒先自证尺子不夹紧）。把 102 张压回 768、请站点开宽版布局、还是给超宽块加 `useMaxWidth:false` 让它可横滚保住 16px 字，是版式取舍；**第 62 轮操作者选了开宽版布局**，于是拿站点自己的 CSS 在本机复量了那条路给多少：`<main>` 从 768 → **1152**，1116px 那张图实绘从 768（标签 11.01px）→ **1116（标签 16.0px）**；扣掉与默认布局同比例的内缩（默认 768 列实测段落只有 707px，即内缩约 61px），可用格约 **1091px**，按它复跑全站还剩 **14 张**轻微超宽（1092–1116px，最小标签 15.6px），「24 张 <12px」清零。开关在站点设置里、仓库改不到，所以判据 `COLUMN` 仍是 768，等你切完、`check_live_column.py` 线上复跑出 ~1152 再放宽。详见更新日志第 62 次第六节。最高的一张 1689px，只影响滚动不影响可读性，所以**判宽不判高**
- **章色不只是「带了指令」，标签墨色读者要真看得见**：`tools/checks/check_mermaid_palette.py`（第 85 轮）把 [编写规范](14-templates/style-guide.md) 那一节的三句话落成判决——21 章色表、掺白公式（`primaryColor` 90% / `secondaryColor` 78% / `lineColor` 45%）、`primaryTextColor` 的字面值**全部从读者能看到的那一页现解析**：规范改了判据跟着改，那一节被删掉或读不出表，判据报 `GUIDE-RULES`「尺子断了」而不是「0 问题」。公式先复现规范自己给的那行 02 章示例（`SELF-ANCHOR`，复现不了就不出数）再去量全书。全站读数 `blocks=217 chapters=21 contrast-pairs=868 | MISS=0 UNPARSE=0 THEME=0 NOCHAPTER=0 WRONGBASE=0 FORMULA=0 TEXTCOLOR=0 CONTRAST=0`——**217/217 带指令、逐块取所在章主色、三档掺白全部对平，这几句从此可复跑而不是人抄**（此前 style-guide 里那句「217/217」是 2026-09-22 的一次手工复核）。比字符串更硬的一条是**读者能不能看见**：标签色与它所在底板的 WCAG 对比度必须 ≥4.5:1（小字档，与自绘图那根轴同一把尺）。**首趟就是被这条抓出 1 个真缺陷**：[长上下文退化](06-memory-rag/long-context-degradation.md) 把三个文字变量都设成本章最浅那级掺白 `#F5FBF9`，量到 **4 组 1.00–1.19:1** 的标签/底板（其余 864 组最低 10.84:1，也就是说这不是分布尾部而是白底白字）；修复前线上 HTML 里 `F5FBF9` 出现 4 次、`1F2937` 0 次，读者拿到的就是那一份。修法只改 4 个颜色字面量，**图与正文一个字没动**：同一张漏斗图真渲染前后两张 PNG 自然尺寸都是 358.5×894px，而**墨色像素 0 → 4457**（判的是落地像素的亮度，不是指令字符串）。绿色由 13 条控制撑着（8 种种植缺陷 + 2 条锚点 + 3 条线上腿自己的尺子）：其中 `#374151` 那条钉住 TEXTCOLOR ≠ CONTRAST（可读但不规范只报一条，不翻倍制造噪声），`#F5FBF9` 那条重演本轮真缺陷必须双报，「拿别章主色」「掺白差一档」「章不在色表里」各归各桶；另有三条钉线上腿的比对有没有判别力（读者页面把指令行嵌在编辑器文档 JSON 里、引号全带反斜杠转义，所以**原样子串永远不可能命中**——第一版腿就是把 6 页全报成缺陷、其中 5 页本轮没碰过，才发现找错了字符串；现在先剥转义再比，并钉「改过一处章色的幽灵指令不得命中」「别页的指令行不得命中」，抽样里一条都没命中时报 `LIVE-BLIND` 退出 2 而不是读成通过）。**落库过程中这把尺子自己错过三次，三次都改在尺子上而不是内容上**：把 `%%{init:` 写进 `%` 格式化字符串会被吃掉一个百分号，5 条控制全部撞进 MISS 而不是各自的桶；Python 的 `round()` 是「四舍六入五成双」而全书 217 块用的是半进位，口径不对就凭空造出 **94 个** `OFF_BY_ONE` 假阳性；表格单元格正则吞掉下一格的 `|`，21 章只读出 14 章。**留下的账**：`--live` 腿按抽样跑（N 页）而不是穷尽 196 页；`tertiaryColor`/`signalColor`/`noteBkgColor` 等键规范没给掺白比例，判据不替规范发明规则，只由对比度腿管着；`actorTextColor`/`noteTextColor` 的字面值同理——规范钉了 `primaryTextColor` 一个，其余三个只受 ≥4.5:1 约束
- **154 篇知识/资源页全部有「参考资料」**（按 frontmatter `type` 统计，不是靠肉眼挑）：339 条去重后的外部一手来源逐条点开核对（arXiv 编号用 export API 比对论文标题），站内跳转一律不混入该小节
- **规范写「硬下限，不达标不得 `published`」，那这句话必须机器问得出答案**：`tools/checks/check_genre_floors.py`（第 86 轮）只认 [编写规范](14-templates/style-guide.md) 第八节那张表做规范来源——每行体裁名后面必须钉一个 `` `type: 键` ``，脚本按 frontmatter 的 `type` 找档、按**正文中文字符数**（围栏块整块剔除）判达不达线，读不到绑定就退出码 2 报「尺子断了」而不是「0 问题」。首跑读数 `GUIDE-BLIND：5 行、其中 0 行钉了 type 键`：旧版那张表一行都没绑到键上，`type: lab` 的 6 篇连一行都没有。本轮把 6 行全绑上键、补上实验型一档（下限 1200 是量出来的：6 篇实测 1313～1766 字，最薄那篇留 113 字余量，而不是把线画在它脚下），修后读数 `pages=196 published=196 graded=159 免检(不限)=37 不可判(同键高档)=40 | problems=0`。**`knowledge` 一个键同时压着原理/概念/实战三档，判据只能按最低那档硬判**，「过了 600 但没到 1200」的 40 页照样打出来不假装量到了；`--selftest` 10 条控制项（8 条把尺子弄坏或把缺陷藏进去 + 2 条锚定合规范本必须绿），每条都做过变异验证
- **323 个项目索引**（实测 star 与许可，头部项目配手写点评）· an index of 323 projects with observed stars and hand-written takes
- **257 张页内配图**：217 个 Mermaid 内联图（全部带章节配色）+ 32 张页内自绘 SVG（含站内横幅与 4 张高保真工具界面示意）+ **8 张真实产品界面截图**（Langfuse、MCP Inspector、OpenHands、Dify、Open WebUI、Arize Phoenix、AutoGen Studio、LangSmith，逐张标注来源 URL 与访问日期）· 257 in-page figures, parser-verified diagrams + real product UI screenshots。按**放置次数**算是 262 处，差的那 5 处是 4 张 SVG 与 1 张截图跨页复用；另有 3 张自绘 SVG（封面与两枚站标）只在站点设置与仓库 README 里用，不在正文页，故不计入配图数
- **215 条读者可见图注**：每张 SVG/截图下方都写了一句「读这张图要带走什么」——因为线上量到 GitBook 会把内容图片的 `alt` **属性清空**（`alt=""`），只在 `<figcaption>` 里把作者写的 alt 印出来（第 58 轮实测，此前这里的说法是半对的）；45 处图片放置里 44 处带注（另 1 处是首页装饰横幅），217 张 Mermaid **171 张带图注、46 张由图前或图后实质引导语解释，逐块量过：0 张裸图**，**没有一张图是「如下图所示」四个字打发的**。这三个数从第 76 轮起有判据可复跑：`tools/checks/check_figure_explanations.py`，读数 `pages=197 diagrams=217 placements=45 captions=215 caption=171 guided=46 image-caption=44 decorative=1 quoted=8 findings=0`（`captions=215` 就是 171 + 44，图注总数由判据算出而不是手抄）。**在此之前它们只是本首页的一句人话**——和第 57 轮「README 引用的判据脚本压根不在仓库里」、第 61 轮「叫读者去看出处却不给链接」是同一类缺陷：数一旦因为后人挪动图块而失真，句子照样读得很自信。判据每个图块与每处图片放置各给一个判决，算覆盖的只有 CAPTION 与 GUIDED 两种，剩下的缺陷共六种（图块侧 BARE / VAPID / VAPID-CAPTION / THIN-CAPTION，图片侧 NO-EXPLANATION / VAPID-IMAGE-CAPTION），而「实质」有一条量化的线：剥掉标点后信息单元 ≥12，光一个指示词（`如下图所示`、`见图`）买不到覆盖——诚实说明：当前树上最短的载体是 23 个单元、且全站 0 行以指示词开头，所以这道线**今天一条都没裁到**，它是防未来某轮写下短载体的回归线，不是给现有 217 张图分类的分界线。图注与引导语可以在图前**也可以**在图后，标题、列表项、表格行、`{% hint %}` 与另一张图都不算载体。写给读者看的图与写给作者看的写法示例也要分开：全站 **8 处**图片写法出现在行内代码里（本首页与更新日志解释这条规则时各写过一处；本轮之前是 7 处，写这段字时一度读到 9，删掉本首页那个行内示例就回到 8——一个措辞就能挪动这个数，这正是 `QUOTED` 只能钉**地板（≥7）不能钉等号**的原因：解释一条规则的文字必须引用那条规则的语法，钉等号等于对「把这条规则写下来」收税。真拦住「把读者见的图塞进反引号躲判据」的是 `placements == 45` 这道等号，加上「被引号包住的图片写法，alt 只可能是 `alt` 或空」那条断言）；判据把它们记成 `QUOTED` 而不是图片放置——复用第 57 轮那份共享分词器（`live_aria_manifest.strip_code_spans`），**不靠改措辞买绿**。反过来**没被引号包住的死路径不再是豁免**：它是缺陷判决 `UNRESOLVED`（本轮读数 0）。这条边界是本轮自己踩出来的——判据落地时把自己那条说明数成了又一处放置。绿色由 23 条种植反例（图块侧 7 + 图片侧 9 + 线上腿 7；双向：裸图必须被抓，图注与引导语必须**不**被抓）+ 真实页变异控制换来：删掉一张真图注，该页 findings 必须 `0 → 1 → 0`；7 页里 6 页如期暴露，另 1 页删掉图注后仍读 GUIDED——它有实质引导语兜底，**这是判对而不是判漏**，控制腿因此只数「图注是唯一载体」的那些块。`--live` 腿落地时只量图块那一半（抽 12 页、比 13 条图注），本轮把它**补成两半并且图片那一半跑成穷尽**：全站 36 张带图片图注的页面、**44 条读者可见的「读这张图要带走什么」逐条与线上对平，一条没少**（读数 `pages=48 captions=86 diagram=42 image=44/44 on 36 image pages still-absent=0 problems=0`）。图片侧此前**没有任何线上判据**：离线的图片轴只配对 `<img>` 元素是否被平台丢掉，这条腿的图注半边只看 Mermaid——而「这张图缩到多少倍、想看清得点开」这类话恰好全住在图片侧。这条腿第一次跑时把 8/8 报成 LOST，量的是尺子不是书：它拿带标点的原文去比 `wl.visible()` 的输出，而后者会把 `《`、`：` 和每个标签边界都换成空格，所以任何图注都不可能匹配。现在两侧同一套归一化，且每页先拿**这一页必定发布的 H1** 做正控制（找不到就报 BLIND 探针故障，不报丢图注），再用一句从没写过的幽灵文本证伪；两种到达形状（图注是 `<i>` 段落、图片说明是 `<figcaption>`）各埋各的反例，其中也包括「拿未归一化的原文去比必须判不出来」这条——否则下一个人会把归一化当成多余的好心删掉。**同一个尺子毛病本轮第二次现身**：第 75 轮那次一次性抽查脚本正是拿带 `「」：、。` 的原文去比，把 9 条完好到达读者的读图提示全部报成丢失，还顺手断言了一件平台从没做过的事（实测：页面角标印的是**平台自己的每页修订时间**，而且是相对时间「1 hour ago」，作者写的 ISO 原文在可见正文里 0 命中——那句抽查断言的前提「页面会印这个字段」不成立；详见更新日志第 76 轮附记）
- **线上图片真的打得开**：`tools/checks/check_live_images.py` 把本地写的每一张图与**线上页面**实际吐出的 `<img>` 对平（漏图 / 多图 / 图注没到页面 / 图片 404 或非 `image/*` 或小于 200B / SVG 被转成位图丢动画），全站读数 `pages=36 refs=45 唯一资产=40（32 SVG + 8 PNG）problems=0`。绿色是用**线上变异控制**换来的：把一页的作者侧改坏，EXTRA + DROPPED + NOCAPTION 三条必须同时响
- **标题里没有会被当成标记的符号**：GitBook 会把每个标题的文本**再印一遍**放进侧栏与「本页目录」，那一份**丢掉行内代码格式**——所以标题里写 `` `$$` ``，正文看着是代码，目录里读者看到的是裸 `$$`。这条线上量到（更新日志 2 处，SSR 全站对平轴 `authored=174 checked=174` 里唯一的一条 LEAK-NAV），离线由 `check_structure.py` 的 HEAD 判据守住：围栏外的标题行含 `{%`/`%}`/`$$` 即报，修复后读数 2→0，全站 198 页 0 问题
- **更新日志不许吃掉上一轮的条目标题**：`tools/checks/check_changelog_headings.py` 把 HEAD 里那份本页的条目标题集合与工作区比，只报「HEAD 有、工作区没了」。这条是被自己的事故逼出来的：第 59 次的提交吃掉了第 58 次的条目标题，正文还在，所以 198 页结构轴、公式轴全部照绿，而整轮记录会挂到下一轮的标题底下、本页目录少一条。「编号必须连续」这种更聪明的判据量不出来——全站编号本来就有 8 个洞（那几轮做了事没写日志）。真实数据两腿：`HEAD~1` 对坏的那次提交 = **1 条丢失**，对修好的工作区 = **0**；判据内部还有四条反例（吃掉中间一条 / 原样不动 / 删掉尾条 / 编号有洞必须读成干净）
- **线上对平轴的读数分两桶**：`tools/checks/live_aria_manifest.py --all --headless` 把「内容对不平」（SSR-PARITY / LEAK / LEAK-NAV / SHAPE / FORMULA）记进 `problems`，把「这一页压根没抓到」（读超时）另记 `fetch-failures`；**两桶任一非空都 `exit 1`**——没抓到的页面什么都没证明，不能读成绿。分桶之前它们共用一个计数，全站两次复跑先读出 `problems=4` 再读出 `problems=3`、命中的页面还完全不同，而两条里的内容级发现都是 0：同一个数字既能表示"网络抖了一下"又能表示"读者少了一个组件"，下一轮就会有人去修噪声。分桶后 `authored=174 checked=174 problems=0 fetch-failures=0 fallback-pages=0`，这是这条轴第一次读到"0 内容问题 + 0 未检页"。四条常驻反例钉住桶的边界：抖动的 fetch 不得进内容桶、真的对不平不得躲进 fetch 桶、匹配页必须算作已检、缩水页留在内容桶
- **行内代码里不写转义反引号**：反斜杠 escape 不了代码跨度的定界符，跨度会在第一个反引号处提前闭合，被丢下的两个定界符再由 KaTeX 配成一对——**中间那句话渲染成空公式，直接从页面上消失**。这类缺陷线上量不出来（同页 HTML 里 184 个 `$$`，读者可见正文 0 个，LEAK 与 KaTeX 计数对平两条轴都不响），所以闸放在作者侧：`check_structure.py` 的 BT 判据对围栏外含转义反引号的行即报。全站该写法实测只有 1 处（就是上一轮写进更新日志的一句），修后 1→0，198 页 0 问题
- **跨页不重复印同一句话、同一条公式**：`tools/checks/check_prose_duplicates.py`（第 60 轮落库）把 191 篇正文切成「句子 / 公式」单元，两两比字符 6-gram 的 Jaccard 相似度。**两遍**：整单元完全相同走 EXACT（不设上限），近似相同走 NEAR（跳过被 25 个以上单元共享的热门 shingle——不加这道限，一句被 30 页抄时判据反而全哑，第 60 轮的探索版就栽过）。范围剔除更新日志（按设计要引用页面原文）、`SUMMARY.md`（导航标签必须等于页标题）与「参考资料」「相关知识点」两节（引文与互链本就重复）。全站读数 `pages=191 prose_units=7414 formula_units=274 dup=0 near-band(0.72–0.85)=3`（`prose_units` 随正文增长而变，第 60 轮落库时是 7104，第 63 轮复跑是 7117，且本首页自己算一篇——改这段字它就会再动。第 83 轮复跑读到 **7362 → 7365**、第 84 轮写日志后读到 **7376**、同轮第一次收尾那一节写完再复跑仍是 **7376**、第二次收尾（就是改上面那句围栏读数的那一次编辑，本首页自己算进语料）读到 **7377**，而那句印的还是第 63 轮的 7116：**这条轴的读数当时没有任何判据盯着**，20 多轮里被后来的正文一格一格推上去，句子照样读得很自信——与第 77 轮那个手抄的 251 同一类。而把 7116 改成 7362 那一次编辑本身又连着把它推了两格，所以这里的数**只到「本轮最后一次复跑」为止**，它不许当等号用。第 85 轮漏跑了这条腿，于是句子带着 7377 上线而 HEAD 那棵树当场读到 **7393**；第 86 轮把它锚回读数，同一条腿顺带抓到隔壁那条 `link-graph` 徽章同一种病（首页 1578 / HEAD 树 1581），两条都是**红过一轮才被发现**——「收尾清单里没有的判据，就是会自己挪数的判据」）；本轮把量到的 **6 条跨页复述公式**改成「白话结论 + 指向权威页」，公式数 928→927，而**被指向的那两页一条定义都没删**。绿色由两组控制撑着：合成语料 5 条反例，外加拿 6 张真实页「种一句话再撤走」的 0→1→0 变化量（第一次种在页尾落进了被排除的「相关知识点」节、第二次种在第 1 行落进了 frontmatter，两次都读出"干净"——那是判据的假阴性，不是内容的清白）。近重复带里剩 3 条、最高 0.778，全是模板页与章索引页互说「收录标准 / 配图要求」的短句，语义上本就该一致，记为 advisory 不判红
- **公式和表格要放进读者真实的那一列，不是设计稿的宽度**：第 63 轮新增两条轴。`tools/checks/check_content_overflow.py` 把 **276 条 display 公式**过真 `katex@0.18.7` 渲染成浏览器 HTML，塞进与线上同款的 `max-w-3xl` + `overflow-x-auto` 盒子里，用无头 Edge 量**墨迹宽**（不是盒子宽——KaTeX 的 `.katex` 是 `display:block`，量盒子永远"放得下"）；修复前 2 条越列（自然宽 **877.7 / 862.7px** > 768），改成 `aligned`  stacked 后复跑 `measured=276 over-column=0 max=766 render-errors=0`，**一个字都没删**。**第 73 轮给这条轴装了第二条腿**：读者那一列不是一个数——线上真浏览器在 1280px 窗口被章节侧栏 288px + 页内 TOC 256px 吃掉，`<main>` 实测 **608px**（手机 358 / 1024 窗口 624 / 1440 起才回到 768），而**任何自己渲染"抓下来的线上 HTML"的轴永远读 768**（客户端路由在 localhost 不水合，导航面板根本不挂载），于是把读者余量往好处多报 160px。现在一趟扫两列（`--column 768 --laptop-column 608`，`0` 退回单列），逐列各出判决：`over-column(768)=0`、`over-column(608)=1`，本轮把 **11 条**在 608 列里要拖动的公式重排（最宽 767px、拖 159px → **0 拖**，重排后最宽 496px），旧 LaTeX 从 `git show HEAD:` 取来重量，不靠笔记。同一轮目检还抓到一条**解析零报错、`render-errors=0` 照样绿**的静默缺陷：amsmath 的环境选项 `\begin{aligned}[t]` KaTeX 不实现，于是 `[t]` 两个字符被老老实实画给读者——守卫 `stray_markup()` 与常驻反例 `PLANT_OPT` 一起落库，`stray` 非空即 `exit 1`。这条轴还推翻了自己最初的假设：原以为居中溢出的**左半边**滚不到（`scrollWidth` 只数末端），实测种一棵 2807px 的雷读出 `lostLeft=0 lostRight=0`——Chromium 把溢出全放在末端且能滚到，所以真实缺陷是**拖拽距离**，`unreachable` 每轮恒为 0 并被常驻断言钉着（哪天不再为 0 就是平台改了行为，先重读前提）。`tools/checks/check_table_overflow.py` 管另一半：GitBook 的表格不是 `<table>`，每格 `min-width: clamp(100px, …)` 而容器 `w-fit` + `overflow-x: visible`（**压根没有横向滚动条**），所以列数 × 100px 一旦超过 768 就直接顶穿正文列——离线量全站 `pages=175 rows=2218 cells=7637 widest=6`（6 列 600px 还在，8 列就是悬崖），浏览器侧在最长不可断 token 的 8 页读回 **2131 格、spill=0、bust=0**。0 是拿控制换的：同一格塞进 65 字符无断点 token，保留平台 `overflow-wrap:anywhere` 读 0 spill、关掉它读 **205~298px** 溢出。这轮还学到一件事并写进判据：带连字符的长文件名**不是**不可断串（浏览器在 `-` 后换行），最初拿它当雷时控制哑火
- **自绘 SVG 的标签到读者眼里还剩几像素**：第 64 轮新增轴 `tools/checks/check_svg_legibility.py`。线上把正文 SVG 画成 `<img style="max-width:100%">`，所以**画布比正文列宽就整张缩小，字跟着缩**（浏览器实测：1920 画布 scale 0.4、960 画布 0.8，`<main>` 768）。有效字号 = authored px × min(1, 列宽/画布宽)，对 32 张正文图、1042 个 `<text>`/`<tspan>`（含从 `<svg>`/`<g>` 继承的字号）逐张量：**32 张全部至少有一个标签低于 12px 门槛，956/1042 个标签命中，最小 5.20px**——最险的是**首页封面横幅**（13px 写在 1920 画布上）。`--column 1152` 复跑得 23 张 / 378 个：**宽版布局只去掉缩小，救不了小字**。等比放大图等于没放大，机械 bump 是渲染出来看的（`03-context-budget.svg` 三版真渲染：×1.25 干净、够到 12px 的 ×1.58 撑破药丸框并撞字），所以**逐张重排文字/画布比例属产品级取舍，本轮 0 改图、轴照旧红**（`exit 1`，不为绿灯降门槛）。判据带两条常驻控制：几何 ruler（种 1920/960/400 三张画布，scale 必须等于 `min(1, 列宽/自然宽)`，列相对断言所以开关一开不用改），和 classifier selftest（真实树里 32/32 张全 FAIL，「见谁都报缺陷」与「量到缺陷」输出长得一样——必须种一张 400 画布的**干净图**读 0 命中，外加一张只该捉住 9.5px、不该捉住 16px 的脏图）。这一轮也被自己的口径骗过一次：`"/." in "/" + rel_dir` 会连内容根一起排除，首页整页被跳过，最险的那张图从统计里消失、轴在 31 张上自称跑完——和 README 的「32 张」对平才把它揪出来。**同一趟线上截图又抓到两条**：① 封面横幅里写着「187 页 / 19 章」，而本页第一行是 196 页——统计轴从没抓到它，因为**它读的是 Markdown，横幅是图片**；已改成 196 并给 `check_readme_stats.py` 加了 banner 腿（只从 `<text>` 节点取数，坐标 `x="196"` 不算声明），反例控制就是重演这次失真（196→187 必须报 2 条）；② 第 61 轮的「上线了吗」门 `check_live_sync.py` 只读 changelog 的 `.md` 端点，而 `.md` 比渲染页先同步——实测 `.md` 已是第 64 次、**同一页 HTML 还停在第 62 次**（9,733,032 字节连查 8 次不变，落后一小时以上）。现在两腿都读、**判决取较差的那条**
- **第 65 轮按这条轴逐张重排**：操作者拍板「分批逐张重排」，批 1 是读数最险的 5 张（首页封面 + `03-context-budget` / `04-react-loop` / `05-mcp-architecture` / `07-plan-vs-react`）。等比缩放中性，所以只有两个自由度：**窄画布**（封面 1920→1280，缩率 0.4→0.6）与**抬字号 + 长画布**（正文图最小字号抬到 15px，0.8 缩率下正好 12.00px；装不下就往高度长，缩放只看宽度所以高度免费）。全站读数 **32 张 / 956 个 → 27 张 / 735 个**（批 1 落库时是 734，同轮批 1c 把一个从没生效的 `<br/>` 换成真的第二行 `<text>`，分母 +1、命中 +1），五张图 223 个越线标签清零，`--column 1152` 的 what-if 23 张 / 378 个 → 18 张 / 204 个；**内容一字未删**（只有 3 处措辞被压短，逐条记在更新日志第 65 次）。轴照旧 `exit 1`，因为还剩 27 张
- **读者拿到的不是仓库里那份 SVG**：GitBook 过资产时跑 sanitizer——重排属性、删注释，并且**剥掉 `paint-order`**、**剥掉 `<animate>` 上取值是 `fill` 的 `attributeName`**。于是 `fill + stroke + paint-order="stroke"` 这种「描边当光晕」的标签在线上变成 3.5px 淡色描边压在 15px 字形上，读者看到一片浅灰残影，而离线字号轴读数完全正常（字号确实是 15px）。第 65 轮线上抽查真渲染才抓到，共 5 处（`04` 3 个、`07` 1 个、`19-lab` CSS 1 个），改成**两层 `<text>`**（底层 `fill="none"` 只画描边，上层只写 `fill`），全是普通表现属性。第二条是**把线上腿跑完 32 张**才现形的：`17-sim2real-domain-randomization` 四个「域随机化」格子写 `<animate attributeName="fill" values="A;B;A">`，到达读者时 `attributeName` 没了 → 没有目标属性 = 动画永不运行，这张图的主题（每格按自己周期换色）在线上死了；同一张图指向 `x`/`opacity` 的 animate 与全站 53 处 `opacity`、11 处 `width` 目标全部存活，所以被剥的不是这个属性而是 `fill` 这个取值（全站恰好 4 处）。同一趟还报出 `<text>…<br/>…</text>`：`<br>` 不是 SVG 元素，**它在本地也从没换出行**（真渲染里那一行字直接压进右侧方框），平台删节点只是把它藏得更彻底——处方不是「绕过 sanitizer」，是改成两个 `<text>`。动画的替代写法用平台确实带的 `opacity` 目标：格子上盖一块同几何、`fill=目标色`、`opacity` 从 0 到 1 的覆盖矩形，且排在前景块之前。断言用**冻一帧再采样**（覆盖矩形设 `opacity="1"`、时变的扫光设 0、删掉所有 `<animate>`）：四格覆盖色与四格前景块色 **8/8 精确命中**——直接截动图会被时变元素污染。判据落库 `tools/checks/check_svg_sanitizer.py`：离线腿（不联网，任何 authored 资产依赖 `KNOWN_STRIPPED`/`STRIPPED_ANIMATE_TARGETS`/`LINEBREAK_TAGS` 里的写法即红）+ 线上腿（逐张取页面上的 `~gitbook/image` 代理地址——`.gitbook/assets/*` 直接 404，拉回服务端副本比对元素与属性多重集，并断言 `viewBox` 相同、副本仍可解析）。控制：selftest 种的属性版与 CSS 版 `paint-order`、`attributeName="fill"` 的 animate、`<br/>` 四种坏写法都必须被抓，两层 text / opacity 覆盖矩形 / 两行 text 三种**修法**必须 0 命中（否则判据会把未来的自己一起锁死）；比对数 <25 直接失败，「干净」不许来自空样本。读数 依赖被剥写法 **1 → 0**、`served copies compared=32 of 32`
- **第 67 轮线上复核时这条腿自己露了馅**：它三次跑到第 30 个资产就被 CDN 掐断连接（`WinError 10054`），而把死掉那条 URL 单独抓一次**第一次就正常返回 5,234 字节 `image/svg+xml`**——是速率不是内容。旧行为有两处错：整条腿 `raise` 出去就停在这里；更要命的是「比过 30 张、还有 5 张没够着」与「35 张全干净」输出长得一样，`checked>=25` 那道地板拦不住它（30>25 照样绿）。现在扫描每资产节流 0.8s、失败先冷却 15s 重试一次，并把**内容桶**（被剥属性 / 非 SVG / 新增的 viewBox 不符，后者不再一响就掐断整趟）与**覆盖桶**（`fetch-failures`）分开记账，**任一非空都 `exit 1`**，覆盖桶明写「这不是内容发现，重跑，别读成绿」。控制两腿：`coverage_selftest` 钉住桶的边界（35/35 绿、**30/35＋5 次失败必须红**、空样本红、`--only` 不被全树地板误伤），外加真循环反例把传输层换成「必失败」演两遍——两张全死与两张死一张都必须读红，且内容桶保持 0。改完复跑 `compared=32 of 32 fetch-failures=0`，并把第 66 轮那把几何尺子喂给**读者拿到的字节**：8 张重排图 `problems=0`，served viewBox 就是本轮新画布（960×624 / 960×556 / 920×404），这一条同时排掉「线上还是上一版构建」
- **标签的墨迹真的落在框里、没有压在连线上**：第 66 轮新增轴 `tools/checks/check_svg_fit.py`。第 64/65 轮只量字号，而**缩小之后仍然看得见的字可以是重叠的**——一个标签可以印在别人的方框上，一段说明可以正好躺在回线上，这些「谁都不会响」。这一轮的判据不估字宽：把图内联进本机页面，让无头 Chromium 真排版，对每个 `<text>`/`<tspan>` 取 `getBBox()`（紧墨迹盒）再乘 `getScreenCTM() × 根 CTM⁻¹` 映回**这张图自己的 viewBox 单位**，于是 `transform`、`text-anchor`、缩放全部自己消掉。四种判据：**BURST**（标签边缘越过自己所属容器 >2px）、**COLLIDE**（不同字符串墨迹盒 x 向重叠 >2px 且 y 向 ≥ 短者高 45%）、**OFFCANVAS**、**STRIKE**（`fill:none` 的描边线按 ~2px 采样，任一采样点落进标签墨迹盒）。这条轴是先杀了一个前辈才立起来的：同一棵树，用「按字符类估宽度、忽略 transform」的探测脚本量出 **563 处**碰撞，而真渲染一张都没有——量不准的东西没资格判定美丑。首趟读数 **8 处**（全是 STRIKE），分布在 4 张图：`17-vla-loop` 3 处（反馈回线横穿三个方框、动作块图例印在「伺服 / WBC」卡片上）、`16-sandbox-layers` 2 处、`06-rag-pipeline` 1 处（Top-K 说明压住虚线返回线）、`05-mcp-architecture` 2 处。最后两条是**判据的假阳性**而不是图的缺陷：GitBook 房风用法是「药丸底 <rect> 先画、连线后画、标签最后画」，被不透明底板盖住的采样点不该算刺字——加上 paint-order 底板豁免后 8→6，剩下 6 处逐张改几何（回线绕空泳道、图例上移、标题下移一行），**没有一处靠删文字解决**。全站读数 **8 → 0**（`figures with a fatal problem=0 of 32  total=0  clearance notes=3`，`exit 0`）。绿色同样由控制换回来：四种脏写法各种一棵必须各自被抓，另有一棵 **PLANT_CLEAN** 把本轮引入的全部豁免（两行分框、绕行走廊、两层字光晕、`<!-- fit:strike-ok ✕ -->` 意图豁免、药丸底板）全用一遍，**必须读 0**——不然豁免只是遮罩。那条意图豁免写在 SVG 注释里，而**第 65 轮已经量过 GitBook 会剥掉注释**：所以它只在作者侧生效，读者拿到的图里什么都没有，这正是它该待的位置（判据读的是仓库里那份 authored 资产）。同期回归：字号轴 27 张 / 735 个 → **25 张 / 686 个**（改几何顺手把两处小字挪进空位），结构轴 198 页 0 问题，5 张改动图 768px 真渲染逐张目检。已知盲区（诚实记账，未修）：`STRIKE` 看不见「字压在别人的卡上」这种非描边交叉，列宽仍钉 768，等宽版布局开关落地后要用 `--column 1152` 复跑
- **字号轴批 2＋批 3（第 67 轮）**：操作者批的「分批逐张重排」继续往下走，21 张手绘 SVG 重排后全站越线读数 **25 张 / 686 个 → 3 张 / 214 个**（分母 1089→1096：7 处长标签拆成两行，标签变多而越线变少，**一个字都没删**）。三个自由度各自到位——抬字号＋长画布（920/960 画布最小 15px、980 画布 16px）、拆行（`09-framework-map-ui` 六张卡全部两行）、换宽（`09` 决策胶囊 236→256、卡片 300×60→290×80、画布 560→624；`16-continuous-batching` 整条泳道右移 40px）。本轮另外修掉三条图以外的缺陷：**内在尺寸轴**（`<img>` 里的 SVG 不写 `width`/`height` 时 Chromium 报 naturalWidth 300，轮初 10/32 张缺，补齐后加两腿断言：声明必须等于 viewBox + 删掉声明的幽灵图必须读 `None`）、**字号轴的 `<style>` 盲区**（`19-lab-react-loop-animated.svg` 用类字号被 13px 兜底误报 15 个越线标签，补上真实层叠「内联 style > 类规则 > 表现属性 > 继承」并种一张 9/15/17/20px 四写法的幽灵图，selftest 两棵→三棵；这张图最终 0 改动——先验尺子再下判决）、**动画条压字**（右移后「持续满载」BURST 8px，用画布加高解决而不是挪字）。fit 轴全站 32 张 fatal=0，结构轴 198 页 0 问题，统计三腿对平。**未做**：`05-mcp-inspector-ui` / `11-observability-ui` / `10-eval-console-ui` 三张高密度 UI 示意图剩 214 个越线标签（不是抬字号能救的，行高列宽面板要按 1.25× 一起长＝重画三张 UI，单独一轮）；21 张里只有 8 张过了肉眼，其余 13 张只过了判据；`16-continuous-batching` 两处白字在动画相位里会先于底色出现，**动画时序可见性**这条轴当时还不存在（第 69 轮已落成并清零）
- **重画一张图不许删掉一个字，而且这件事要能量出来**：第 68 轮收掉字号轴最后 3 张高密度 UI 示意图（`11-observability-ui` / `05-mcp-inspector-ui` / `10-eval-console-ui`），全站越线读数 **3 张 / 214 个 → 0 张 / 0 个**（分母 1096→1109，重排**加**出 13 个标签：状态条拆两行、补一枚「共 4 个」计数、括号限定语单独成行）。这三张不吃第 67 轮那三个自由度——它们原本 960×560、缩率 0.8，要把 12px 门槛抬回去得写 15px，而面板行高列宽是按 12px 排的，抬字号必撞框。于是换第四个自由度：**把画布宽度改成读者那一列的 768px**，缩率正好 1.0，作者写几像素读者就看见几像素，高度 724/724/674 免费。判据 `tools/checks/check_svg_label_parity.py`：先剥掉标签嵌套/实体/分隔符（`·/—→（）:，。、|+×`）与空白，再要求**内容字形多重集**旧−新为空。为什么不用标签集合比：重画合法地把长标签拆成多行（JSON 逐行、两行状态条），逐标签比会把拆行读成删除；拆行与换标点逃不过字形集，**改名却逃得过后门没有**——改名必须写进 `REWRITES` 并给理由、新词必须真在图上，判据每用一次打印一次，否则「不得不改个说法」就成了让重画悄悄瘦页的逃生门。控制腿五种：整句删掉、标签缩字、未声明的改名、丢一个拉丁词各自必须报，同份标签自比必须 0，声明过的改写必须读干净。它**当场抓到两处真删字**（`05` 状态条一行只装得下 666px 却需 729px，我第一次直接把「最近调用 42 ms」删了 → 改判两行状态条；`11` 搜索框占位被压短丢了 "trace"「搜索」 → 把搜索框搬进标题栏，位置也是自由度），也**抓到判据自己一个 bug**：`re.escape` 作用在含 `"\\s"` 的字符类上会让判据把**字母 `s` 当空白剥掉**（输出里出现「pan 树」「decription」，`deleted-glyphs` 全是空格），任何丢 `s` 的删字都会隐身——改分两步剥并加显式断言 `norm("span 树 · 1（秒）")=="span树1秒"` 钉住。全站读数 `figures audited=3 deleted content=0`。同期回归：fit 轴全树 32 张 `fatal=0 total=0`（三张新图 problems=0，种植 4 处各自被抓）、字号轴三条控制＋线上腿全过、结构轴 198 页 0 问题、统计三腿对平、三张 768px 真渲染逐张目检；**线上复核量的是读者那一份字节**：从 `llms.txt` 取页面地址 → 读页面上那条 `<img>` 的 `~gitbook/image` 代理地址 → 把服务端副本内联进 768px 宿主让真 Chromium 排版。三页 served viewBox 与 authored 完全相等（先证伪：`HEAD~1` 的旧画布是 960×560，「线上还在发上一版」这条判断装得上去就一定响），平台自己声明 `width=768 == min(768 列宽, 768 画布)`，**实测缩率 1.000**——本轮的核心主张从仓库里的算术变成读者字节上的读数；三张 768px 截图逐张看，`05` 的两行状态条在线上确实带着「最近调用 42 ms」（就是第一次改法悄悄删掉的那句）。这条腿也修了自己两处错：手写 `unquote` 解代理地址被 CDN 判 **400**（可请求形式只能 `html.unescape`，内层 `%252F` 是签名的一部分——仓库里的线上图片轴早就把这件事钉成 hit control，正解是复用它而不是再实现一遍解码），探针回传的 JSON 漏了信箱配对的 `id`，于是三页齐读成「浏览器腿没量到东西 = 覆盖缺口」并 `exit 1`（它把空样本报成失败，正是该有的行为）。目检还抓到一件尺子量不到的：`10` 第四根柱的标签「长文问答·跌」是 `HEAD~1` 就有的旧写法，字号与几何都判干净，只有人眼看得出它像被截断的半句——**措辞可读性**这条轴的第一个样本，本轮不动它。**未做**：第 67 轮那 13 张只过了尺子没过肉眼；`16-continuous-batching` 动画相位里白字先于底色出现（**这条轴第 69 轮落成并把两类判决全部清零**）；字号轴仍不看 `transform="scale(k)"`（全站无人这么写，是盲区不是缺陷）；**线上那条读者侧浏览器腿目前是一次性探针**（页名写死三张，要常驻得补 selftest 落进 `tools/checks/`）；宽版布局开关还没落地（`<main>` 实测 768），它一开这轮的 768 画布决策要用 `--column 1152` 重读
- **字在某一帧是看不见的，而静态帧那几根轴全都量不到**：第 69 轮新增轴 `tools/checks/check_svg_phase_legibility.py`。此前的每根 SVG 轴只看一瞬间（字号轴比 authored px 与列宽，几何轴在冻结帧里量标签框），可 32 张图里 14 张在动（100 个 `<animate>`），而动画有静态帧显示不出的看不见法子：**在长的条**（`values="0;300;300"`）底下那行白字，在条到位之前是白压白；**在淡入的遮罩**把标签埋掉半个周期；**面板自己的 `opacity`** 在动，读者的底色是晚到的。冻相位这个手段是量出来的不是假设的：`pauseAnimations()` + `setCurrentTime(t)` 用 `setTimeout` 步进，**且必须带 `--virtual-time-budget`** 才落在作者写的坡道上（0→100 / 2s 的条在 t=1.0 读 50、t=1.9 读 95）；没有这个 flag 整页不回 beacon，用 `requestAnimationFrame` 步进则 25 相位只跑到第 1 帧。判据算的是**读者真拿到的堆栈**：自底向上合成页面白 → 标签之下每个绘制面（各自 alpha 再乘祖先 `opacity`，半透明板是一层真实的部分覆盖所以不能跳过）→ 墨色盖上去按 WCAG 相对亮度比对比度；渐变按 `<stop>`、图案按 tile 定价，取**离墨色亮度最远**那端（给标签最有利的一次读数），所以 DARK 判决永不可能是服务器的锅，这类行记 `approx`；引用悬空的板报**未定价**并计数——量不了的板不许读成通过。**埋没是两臂测试**，两条阈值都在这棵树上量的：`--buried`（6 个探针点里同一相位被同时藏住的占比）与 `--steady`（一个点在整个周期保持被藏的占比）——播放头扫过被裁切的标签是 2/6 点 × 1/39 相位、旅行的数据包 1/6 × 6/35（编排），而 `04` 中心圆盘在 **25/25 个可探测相位**咬掉一个徽章字形（真 z 序缺陷），**单用任何一臂都会把这两类判错其中一类**；`COVERED` 报的是标签**上面**那层板。标签自己在淡入时不作对比度判决：alpha 低于 `--presence`（0.60）的相位豁免并计数，这样"正在淡入"和"根本看不见"分得开。因为每帧都采，判决能说清是 `PHASE-ONLY`（最好帧过、最差帧不过——静态帧轴完全看不见）还是 `DARK`（稳态越线，顺带抓到，因为旧轴一根都没量过颜色）。读数：门槛 3.00 首趟 **45 个 / 9 张**（3 相位专属 / 39 稳态 / 3 埋没）→ 美术批 A 43 → 批 B 21 → **0**；清零后门槛灵敏度 3.00→0、3.50→**8**、4.50→**66**（这行是反证腿：0 不等于尺子不设门槛）。尺子自己改了两处，每处种反例：**图案 tile 按自身 `opacity` 折算**（房风格发丝网格 0.7px @0.22–0.5 按全强度定价 = 宣称 `06` 每个标签坐在不透光石板上，**5 个假 DARK**；`PLANT_TILE` 种一张不透明图案必须响）、**WCAG 1.4.3 大小字分档**（`SCALE = svg 实宽 / viewBox 宽`，960 画布在 768 列里 0.8；落地 px ≥24 或 ≥18.66 且字重 ≥700 → 门槛 3.0，否则 4.5；这一改把轴从 43 个判决变成 43 + 66，不分档就是拿错门槛判决，四张档位幽灵图各钉一头）。此外 `begin="indefinite"`（时间尺推不动）必须报**尺子瞎了**而不是干净，每图活体检查要求第一个 `<animate>` 下的节点在周期三点读数互不相同（否则是**覆盖缺口**不是通过），`MIN_FIGURES=10` 让子集跑不许声称全站干净；`--selftest` 16 张种植全对。分档尺子下 **66 个 / 12 张 → D1 只改墨色（16 处断言式改写 / 12 文件）→ 22 → D2 改底板（14 处 / 6 文件）→ 0**（未定价 0、覆盖 0，PRESENCE/BURIED/STEADY 一条没放松）。落下的美术规则：**带字的底板必须坐在该色相 -700/-800 档**（房风格兄弟 indigo-700 `#4338ca`、cyan-700 `#0e7490`；白字要板亮度 ≤0.1833 才够 4.5:1，浅色 tint 墨 `#e0f2fe`/`#fff7ed`/`#d1fae5` 更紧 ≤~0.155–0.165）；`03` 输出 3.77→7.68、`04` 三张卡 3.00–4.10→5.48–7.68、`08` 主管 3.56→7.31、`16-batching` 4.00/3.60→5.78/5.60。**`--presence` 当过旋钮试、最后留在 0.60**：两条 α 脉冲标签（`16-sandbox` 的 ✕、`17-action-chunking` 的「丢弃剩余动作」）在 0.60 线上只有 3.55，抬线到 0.75 能让它们"过"但那是拿尺子换绿灯——**它会把将来落在 0.60–0.75 的每一个淡入标签悄悄免掉**；正解是改墨色（red-900 `#7f1d1d` → red-950 `#450a0a`），门槛没动就升到 4.56 / 4.78。`16-batching` 那两处走相反方向（底改浅 `#60a5fa`/`#a78bfa`、白字改深墨）——第 67 轮记的那句"白字先于底色"本质是墨与板**同向**的问题。取舍留名：`04` 琥珀胶囊现在偏棕、`08` 更赤陶，为可读性接受。同期回归 7 条轴全绿（结构 198 页 0 问题；fit 轴 32 张 / 1063 标签 fatal=0；字号轴 0 of 1109；label-parity 12 张审计 0 删字；sanitizer 干净；跨页重复 dup=0；统计轴对平 196 页），8 张相位截图逐张目检。**线上复核量的是色值本身**：sanitizer 轴比的是元素与属性多重集，`fill` 从 `#0284c7` 换成 `#0369a1` 对它完全隐形，所以新落判据 `tools/checks/check_svg_served_colours.py`——量 12 张改动图**服务端副本的色值多重集与 authored 逐字相等**（`reached 12 of 12 mismatched 0`，单张最多 123 个色值字面量；判据是"相等"而不是"新色出现过"，否则少用几次某色也会读成通过），反证腿是**拿改动前那版画法去比同一份服务端字节立刻 DIFFERS**（默认首张资产 `03` 读短 14 / 多 14，`04` 短 19 / 多 19；控制腿还断言上一版必须与本版不同，否则"DIFFERS"是空话；比对基线不写死 `HEAD~1`——收尾提交一压上去那条腿就会静悄悄量不到东西，改为工作区脏比 `HEAD`、干净比最近动过资产那次提交的父本，两个分支都现场测过）——绿因此说的是"读者拿到了第 69 轮"；覆盖与判决分桶（`--min-reached 10`）。更新日志页线上顶格即第 69 次；`08`/`16` 整页 1280px 截图 + `04` 服务端副本内联进 768px 宿主真排版逐张看。这一腿量到两条工具边界：headless Edge 带 `--virtual-time-budget` 直取 `react` 整页会**挂死**（无限 SMIL 时钟让虚拟时间永不 settle，同批 `08`/`16` 正常），而浏览器 MCP 的 `innerWidth/innerHeight` 读到 **0×0**（页面 `<img>` 排成 12×9）——它能读 `naturalWidth`（`04` 报 960×700，正是 authored viewBox），但不能用来做宽度判决。另外修掉一条**长得像内容失败**的真 harness bug：`check_svg_fit.py` 在 Windows 跑到第 12/32 张死于 `PermissionError`——Edge 进程已退出还留着 WebView2 日志句柄，临时目录删不掉于是整趟没有判决，改 `ignore_cleanup_errors=True` 并写明原因（丢一个临时目录很便宜，丢一趟判决不是）。**未做**：只覆盖 14 张动画图（另外 18 张没有相位可扫，但静态图的颜色目前只被 DARK 顺带管着）；渐变板定价偏乐观（通过不等于它自己最亮 stop 也过——只能漏判不会误判，但确实是口子）；`[on ...]` 报的是最后检查的那层不一定是最差相位之下那层；两条 α 脉冲行只在 presence 边缘过；`--shots` 按虚拟时间推算是近似值（给肉眼用不做判决）；宽版开关一开这条轴的 `COLUMN=768` 要跟着重读
- **读者那一列里，公式要真的画出来、还得放得下——这一条只能在活页上量**：第 74 轮把第 73 轮那段一次性的线上复核落成常驻判据 `tools/checks/check_live_formulas.py`，并加宽到每一篇作者含 display 公式的页（106 页 / 276 条），不再只查改过的 11 页。它量的是读者浏览器真水合出来的那份文档：逐页判四件事——线上条数 == 仓库作者条数、无 `.katex-error`、渲染文本里没有裸 LaTeX 残留（`[t]` 那一类）、墨迹宽 − 滚动盒宽 ≥ 16px。宽度仍按**墨迹**量（`Range` 夹住 `.katex-html`，`scrollLeft` 两端各取一次取大），量盒子永远"放得下"。这条轴与作者侧拟合轴（`check_content_overflow.py`）是两根：前者证明源码放得下，后者证明平台真发给了读者。**尺子带一条"量不到拖动就自杀"的标定腿**——把本页最宽公式的滚动盒用脚本改窄 100px，判据必须立刻读出 ≥16px 拖动，量不到直接 `assert` 死（一个悄悄改量盒子的探针，绿灯和真探针长得一样）。首跑又暴露并修掉三条自身缺陷：① 无逐页预算（`networkidle` 配 90s 会被一个长轮询页白等）→ 改 `domcontentloaded` 45s 硬预算 + 尽力而为的 idle + 2s 沉降；② 「没量到」原来混进缺陷桶 → 内容桶与覆盖桶（`fetch-failures`）分开记账、任一非空都 `exit 1`，覆盖桶明写"别读成绿"（第 67 轮在资产轴记过的同一条教训，这次落在新轴落库的**同一轮**）；③ 看不见进度 → 逐页打印实测值 + 耗时、行缓冲、页间节流 0.8s。还新增一条**隐藏/可见分类器**：`{% tabs %}` 未激活面板里的公式本该无盒（读者要点击），它被报为 `hidden` 不判缺陷，而无隐藏祖先的零宽盒才是真缺陷——分类器自己有常驻反例（`display:none` 副本必须读 hidden、挤成零宽但留在流里的副本必须读 no-reason），所以"hidden"永远无法给一个空盒子开脱。首趟全站复扫把第 73 轮那 7 条「公式没排版」读成了**水合竞态**（探针在 KaTeX 排版完成前就量了，`settled()` 现在先重读最多两次再下判决），不是内容缺陷；全站判决记在更新日志第 74 次。离线 `--selftest` 钉住桶的边界（干净=0、一页没够着=1、真缺陷不许躲进覆盖桶、空样本必须报、`--limit` 抽样不被误伤）
- **引用轴的覆盖率分解：把一条候选判死，比给它写分类器便宜**：第 74 轮给 `tools/checks/check_quote_fidelity.py` 加 `--buckets` 腿。第 71/72 轮反复写下「`attributed=16`，全站 2265 个「」串」并把它列成下轮头号候选（做引用/术语分类器管剩下两千条）。本轮先量再建：`--buckets` 用判据自己的正则把语料摊成 GRADED 6 / SHORT-NAME 1350 / SOURCE-NEAR-NO-VERB 40 / SENTENCE-SHAPED 10 / OTHER 346 五堆，两个"可能藏真引用"的堆逐条读完**没有一条是外部引文**（全是自家用语与我们编的示例问句）。分类器要判的那个缺陷在这棵树里量不到样本，候选判死，改成一条常驻覆盖率腿：以后 `findings=0` 旁边必须同时打印这张表，谁也不能再读成「所有引用都核过」。这条腿**不动退出码**（覆盖账不是判决），内部 5 条种植自证：五种写法各必须落进自己那一堆
- **真实产品截图在读者那一列里还剩多少可读缩放**：第 75 轮新增轴 `tools/checks/check_screenshot_legibility.py`，收掉第 68/70/74 轮日志里抄了三遍的那句「8 张 PNG 从没被量过」。其余图都是自家标记（Mermaid 有几何轴、SVG 有六条腿），**字号能从源码读出来；PNG 没有源码**——图里那行 13px 是别人的 app 画的，我们只能决定这张位图被排多宽。线上实测（真浏览器、水合完、两个 DPR）：8 张全是 1280px（一张 1040px）宽的真实 UI，而平台把每张内容图**钉死在列宽**——图、wrapper、`<main>` 同为 608px，光栅图**没有横向滚动条**（第 73 轮那条只属于 display 公式），于是厂商 UI 的 13px 文字到读者眼里是 6.2px；`~gitbook/image` 代理在 DPR 1 给 768px、**DPR 2 只给 640px**，2x 屏让字形更锐但不让它更大。修法不是裁图（图注读的是整屏，裁就没得读），而是把平台已经给的东西写成可判的事实：实测 lightbox 挂在 `<body>`、取 **authored 字节**（naturalWidth 1280 而非代理的 640）、画 1216px，7 张 0.950、那张 1040px 的 1.000，**8/8 成立**。于是判据两侧都要：排在 authored 0.90 以下**且图自己那几行没告诉读者可点开** → `SHRUNK-UNDISCLOSED`（首跑 9/9 命中，0.475×8 + 0.585×1；本轮在 9 处图注后各补一行「读图提示」，**一张图没删没裁没重排**；第一版 8 张用了同一句话，被第 60 轮的跨页重复轴当场报出 `DUP-EXACT x8`，于是改成逐张写自己的宽、自己的缩放比和点开之后具体读什么，复跑 `dup=0`）；比列还窄被放大 → `STRETCHED`；代理交出的宽度低于 authored → **CAP 只报不判**（稳定读数 18 条 = 9 处引用 × 2 个 DPR：DPR 1 只给 768px、DPR 2 只给 640px（那张竖版 520px），2x 屏交出的位图比 1x 还窄；这是平台侧的取图策略）。豁免不许只用文字买到：`--live --zoom` 必须真量到，否则 `UNVERIFIED` / `ZOOM-MISSING` / `ZOOM-LIE` 三种判决照样判红——**所以这条轴的常驻读法是 `--live --zoom`，离线跑必然不绿，这是设计不是没修完**。正式读数（连跑两遍）`figures=8 told-and-verified=8 problems=0 cap=18 coverage=0 exit=0`，`live <main> widths seen: 608px x18`。第一趟全站跑出的 `problems=2 coverage=3` **全是尺子自己的错**，加上复跑期间冒出来的两条，一共四条都记进判据：① 点开之后我读的是「页面上最宽的 ≥400px 图」，而弹窗里那张此刻还没解码（naturalWidth=0 被筛掉），探针回头把**仍在原位的 608px 列内图**当成放大结果——两张诚实的截图被冤枉成 ZOOM-LIE，读数恰好等于列宽；现在只认 `<main>` 之外、且必须等到 `zoom-modal` 里的位图真解码出来，读不到就分明「弹窗没开」与「开了位图没到」。② 覆盖账按 naturalWidth≥400 数图，而代理给的 naturalWidth 随 DPR **变小**（同一张手绘 SVG：DPR 1 是 960、DPR 2 是 384），DPR 2 天然少数一张；改成「DOM 里有几张 / 几张拿到盒子」。③ CAP 计数复用了 ② 那道 400px 闸，于是**平台压得越狠、这条打印越安静**（两遍全站读数 18 与 17 就是这么来的）；CAP 现在只看「这 URL 是不是仓库里的资产」，尺寸闸只留给目检截图，反例三向钉住（300px 副本必记 / 原尺寸必不记 / 0px 不许冒充 cap）。④ 拆掉闸之后 CAP 开始吃到 `naturalWidth=0` 的行——可 0px 不是平台选的宽度，**是探针没等到位图**；而点一张像素还没到的图，GitBook 根本不开弹窗，于是那两张诚实截图第二次被判成 ZOOM-LIE，判决文本还印出 `scale=-1.000`（哨兵值被当成实测比例）。四处一起改：判据前先 settle 轮询等到「有盒子且拿到位图」、拿不到位图的行进覆盖桶判红、`cap_line()` 见 0px 返回 None、点击前确认 `complete && naturalWidth>0` 且最多做 3 次真指针点击并**打印第几次点开**（「第二次才开」是探针的事，「三次都不开」才是平台没兑现——这条重试在确认趟里当场用上：Phoenix 那张打印 `opened on click #2`，位图早已解码，第一次点击仍然会被吞）。绿色还由 24 个种植幻影撑着：8 张资产各重排到列宽（必须干净）、2 倍（必须 SHRUNK）、一半（必须 STRETCHED）——**「8/8 命中」是一条缩放规则，不是八个嫌疑犯名单**；另有 told/untold 双向、双重编码 URL 必须解析回资产（否则 CAP 腿会像首跑那样把 640/1280 报成 `cap=0`）、豁免四方向。目检证据最能说明问题：那张 Inspector 截图缩到 608px 时我把状态行读成「sunny, **14**°C」，点开放大读的是 **24**°C——同一张位图，宽度就是「看清」与「读错」的分界。**未做**：正文列宽 608px 仍只报不判（第 73 轮起），宽版式开关落地后这张轴要用 `--column 1152` 重读。
- **「最后更新」这个字段可以随便印，而全站没有一根尺子问过它是不是真话**：第 75 轮给 9 页补日期时顺手量出的第二类缺陷，落库判据 `tools/checks/check_updated_dates.py`。`check_structure.py` 只查这个键**存在且格式对**，所以一页可以永远印着任何看起来合理的日期；而 GitBook 会把它画给读者。判据拿 git 逐行比（`-U0`）：宣告的日期 vs「最后一次改了除 `updated:` 键以外任何一行的提交」，**方向对称**——落后 → `BEHIND`（读者被告知这页比实际上旧），超前 → `FORWARD`（读者被告知一次没发生的更新；单向规则等于允许一轮只改元数据就把全站的日期"刷新"）。首跑 reader 页 198 / 计数 196 / 豁免 2 量到 **17 条 BEHIND**，全部是本仓库自己的轮次留下的：10 页第 73 轮的公式重排、1 页第 74 轮改首页、6 页第 52/54/63 轮——**这一类不是第一次**：第 17 轮手工回填过 80 个失真日期，当时没判据，六个轮次之后又攒出 17 个。`--fix` 一次写回 → `problems=17 → 0`、`exit 1 → 0`，`git diff --numstat` 逐页恰好 1/1（**17 页正文一个字没动**，脚本 assert 拦住任何 1/1 之外的页；这 17 页落库后在整轮 diff 里读作 16 页 1/1 + `05-tool-protocol/mcp.md` 的 3/1，那一页本轮既补了读图提示又被改过日期，`--fix` 下笔时它那一条仍是 1/1）。两条让规则不自喂的设计：只改日期键的那次提交**永远不移动**"最后正文改动日"（否则 `--fix` 追自己的尾巴），以及工作树相对 HEAD 超前时 `FORWARD` 落 `pending-commit` 桶不判红（本轮那 8 页今天改了正文、还没提交，git 此刻无法作证），这条带一对反例：同一行读数脏时读 pending、干净时必须 fire FORWARD，而 BEHIND 脏不脏都照判。控制腿共 8 向：相等干净 / BEHIND、FORWARD、NOKEY、NOCOMMIT 各必须报 / 豁免页不进 `checked` / 只改日期的合成 diff 不得移动日期、含一行正文的必须移动 / 多条提交取最新正文那条 / `checked` 地板 ≥190（"没样本"读不成通过）/ 豁免表必须是**闭集且文件仍然存在**（改名或新增文件不能悄悄混进来）
- **2026-09 前沿已对齐**：GPT-6 Astra、Agents API、Claude Fable 5.1、System One 决策模型（Jev）、Terminal-Bench 4.0

查资料用 [资源总表](00-index/resources-index.md) 与 [标签索引](00-index/tags.md)，术语卡住查 [术语表](15-glossary/README.md)。

Use the [resource index](00-index/resources-index.md) and [tag index](00-index/tags.md) to look things up; the [glossary](15-glossary/README.md) covers terminology.

## 许可证 · License

MIT
